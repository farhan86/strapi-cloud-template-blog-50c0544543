'use strict';

/**
 * early-morning-calendar controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::early-morning-calendar.early-morning-calendar');
