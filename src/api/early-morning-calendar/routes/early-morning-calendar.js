'use strict';

/**
 * early-morning-calendar router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::early-morning-calendar.early-morning-calendar');
